[supervisord]
nodaemon=true
logdir=/usr/app/logs
childlogdir=/usr/app/logs

[program:mongodb]
command=mongod --dbpath /data/db --nojournal --noprealloc
directory=/usr/app
user=root

[program:node]
command=/bin/bash ./expose_dev.sh
directory=/usr/app
user=root